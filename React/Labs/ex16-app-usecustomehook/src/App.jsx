import './App.css'

import MultiInput from './register';

function App() {

     //const [inputValue , handleChange , handleSubmit] = useInput("hello",displayData)
     /*
      const [id , handleChangeid , handleSubmitid] = useInput("",displayData)
      <input type={id} onChange={handleChangeid} /> 하나 
     */
  
  return (
   
   
   <div className='App'>
     <MultiInput />
   </div>
  )
}

export default App
